<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Wallet detection queries for WalletConnect -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="wc" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="metamask" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="rainbow" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="trust" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="cbwallet" />
        </intent>
        <!--
            Korea mDL (모바일 신분증). Android 11+ hides other packages unless they
            are declared here, so `Linking.canOpenURL` returned false and the flow
            reported "Mobile ID app is not installed" on a device where it WAS
            installed. Declared by package, not just by scheme: the server hands
            back either `mobileid://` or an https app link depending on the stage,
            and naming the package covers both.
        -->
        <package android:name="kr.go.mobileid" />
        <!-- The Android branch prefers `ssPayLink`, which hands off to Samsung
             Wallet rather than to 모바일 신분증. Both targets have to be visible
             or `canOpenURL` fails on whichever one the server returns. -->
        <package android:name="com.samsung.android.spay" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mobileid" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- ML Kit barcode model auto-download for QR scanning -->
      <meta-data
          android:name="com.google.mlkit.vision.DEPENDENCIES"
          android:value="barcode" />
      <!-- Main Activity with Splash Theme -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:theme="@style/SplashTheme"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Deep link for WalletConnect callback -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="zkproofport" />
        </intent-filter>
        <!-- Microsoft Entra ID OIDC redirect -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="msalcd5404cc-d313-431c-8112-725e629dad28" android:host="auth" />
        </intent-filter>
      </activity>
      <!--
        OpenStoa FCM handler. Subclasses expo-notifications' ExpoFirebaseMessagingService
        (which registers the same action at android:priority="-1"); this declaration wins
        service resolution at the default priority and delegates non-OpenStoa messages
        back to Expo via super.onMessageReceived.
      -->
      <service
        android:name=".openstoa.OpenStoaMessagingService"
        android:exported="false">
        <intent-filter>
          <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
      </service>
    </application>
</manifest>
