# masselabs/, NOT zkproofport/. The old repository is shared with other
# projects, so `match nuke` against it would revoke unrelated certificates too.
# The corporate team gets its own storage rather than sharing one that cannot be
# safely reset.
git_url("https://github.com/masselabs/ios-certificates.git")
team_id("R4G32CVY29")

storage_mode("git")

type("development") # The default type, can be: appstore, adhoc, enterprise or development

# app_identifier(["tools.fastlane.app", "tools.fastlane.app2"])
# username("user@fastlane.tools") # Your Apple Developer Portal username

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
